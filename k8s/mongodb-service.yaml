apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: medflow
spec:
  clusterIP: None  # Service headless (pas d'IP, juste DNS)
  selector:
    app: mongodb
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb

